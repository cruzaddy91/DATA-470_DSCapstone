# Data Science Capstone — dependencies
# Install: pip install -r requirements.txt
# If ``import matplotlib`` fails with a NumPy binary mismatch, use an isolated env:
#   bash setup_venv.sh   then   .venv/bin/python …

# Data and ETL
pandas>=2.0,<3
# NumPy 2.x needs matplotlib>=3.9 wheels; pin range so conda/pip do not mix 1.x-built matplotlib with numpy 2.
numpy>=1.26,<3
pyarrow>=14.0

# Kaggle download (optional)
kagglehub>=0.3

# Jupyter
jupyter>=1.0
ipykernel>=7.3.0
notebook>=7.0

# Analysis and modeling
scikit-learn>=1.3,<1.7
scipy>=1.11,<1.15
statsmodels>=0.14

# Visualization (matplotlib<3.9 may ship binaries incompatible with numpy 2.x)
matplotlib>=3.9.0,<3.11
seaborn>=0.13.0,<0.17

# Gradient boosting (for tabular ML)
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# Optional imbalance handling (CV-safe SMOTE when MODEL_ENABLE_SMOTE=1)
imbalanced-learn>=0.12.0,<0.14

# Utils
python-dotenv>=1.0

# Poster PPTX (fact build, ``build_poster.py`` / ``render_poster_pptx.py``)
python-pptx>=0.6,<2
Pillow>=10.0,<12
