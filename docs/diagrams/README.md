# Architecture diagrams (README)

Pre-rendered PNG for GitHub (no Mermaid pan/zoom). Edit `.mmd` sources, then:

```bash
./scripts/render_readme_diagrams.sh
```

Configs: `mermaid-config.json` (light), `mermaid-config-dark.json` (dark).
